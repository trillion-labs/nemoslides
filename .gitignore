# secrets
.env
.env.local

# python
__pycache__/
*.py[cod]
.pytest_cache/
.venv/
*.egg-info/
.ruff_cache/

# uv
# (keep uv.lock committed)

# data / artifacts
data/raw/
data/**/*.jsonl
data/gallery_*/
data/seeds.d.archive-*/
data/query_cache.json
# data/hf/ is ignored except for the test split, which the gallery
# builder needs in CI (the full dataset is public on HF).
data/hf/*
!data/hf/slides-sft-v0.test.jsonl

# runtime results (generated; comparison_table.md + *_results.json are committed)
results/eval/runs/
results/blindtest/votes.db
results/blindtest/pair_queue.json
results/demo-presentations/

# training outputs
checkpoints/
wandb/
outputs/
.external/

# codex pipeline work dirs (scratch, >100MB of intermediate seed folders)
work/
work_*/
.tmp-codex-*
.data-designer-artifacts/

# rendered artifacts
**/screenshots/
**/dist/
assets/renderer/node_modules/
node_modules/

# slidev render staging files (trap-cleaned; stragglers if process killed)
assets/renderer/.tmp-deck-*
assets/renderer/.tmp-seed*

# local tool state
.claude/
.omx/

# editor
.DS_Store
.vscode/
.idea/

# mkdocs build output
site/
